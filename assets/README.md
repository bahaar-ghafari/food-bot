# Assets

- `foodbot-profile.png` — bot profile picture (1024×1024). Set via @BotFather → `/mybots` → select bot → Edit Bot → Edit Botpic (Telegram has no API for a bot to set its own avatar, so this has to be uploaded manually).
